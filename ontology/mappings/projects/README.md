# Project mappings

Keep site aliases, asset-to-role assignments, command owners, freshness policies and workflow correlations here. Use synthetic examples only in this public repository; customer identifiers and deployment addresses belong in private configuration. Project vocabulary may map to canonical IDs but must not redefine them.
