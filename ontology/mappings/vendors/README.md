# Vendor mappings

Add one versioned file per documented supplier/interface. Record vendor, model, firmware, protocol version, source document, source field, canonical term, conversion, units, unknown handling and validation evidence. Preserve raw values. No vendor mappings are asserted in Tier 1.
